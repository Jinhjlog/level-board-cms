# 측정 기록 — 게시판 CMS 백엔드 (AI-DDD 파이프라인)

> 이 템플릿의 AI 개발 프로세스(Contract-First + TDD + G 병렬 자동화)로 **게시판 CMS 백엔드**를
> 얼마의 시간·비용에 만드는지 측정한 기록. 영상 자막 / 블로그 표 / 레포 근거의 **단일 출처**.

---

## 📊 한눈에 결과 (TL;DR) — _대충 봐도 이거 하나면 끝_

| 게시판 CMS 백엔드        | 수치                 |
| ------------------------ | -------------------- |
| 구현 API                 | **\_\_ / 13**        |
| 총 실시간 (벽시계)       | \***\*h **m\*\*      |
| ─ 사람 active 시간       | **h **m              |
| ─ 에이전트 자동 구간     | **h **m              |
| 토큰 / 비용              | ** tok / \*\*$**\*\* |
| 통과 / 실패 (lint+build) | \***\* / \*\***      |
| E2E (사람 별도 실행)     | \_\_ pass            |

> **한 줄 결론(채우기):** "게시판 CMS 백엔드 **13 API**를 \***\*시간 / $\*\*** 에.
> 기계적 구현(G)은 **\_\_%**, 시간을 먹은 건 사람 판단(게이트) **\_\_%** 였다."

---

## 1. 측정 조건 (Disclosure — 정직 장치)

이 숫자를 읽기 전 알아야 할 전제. **숨기면 뻥, 밝히면 신뢰.**

- **DONE 정의** = `lint` + `build` 통과. (E2E는 사람이 별도 실행, 위 표에 분리 기재)
- **베이스라인** = 이 DDD 템플릿에서 시작. **인증(`admin`/`user`)·파일업로드(`file-upload`)는 기존 재사용** — from scratch 아님.
- **숙련도** = 이 템플릿 패턴을 아는 **숙련 사용자** 기준 속도 (= 누구나 이 속도 아님).
- **사전 설계** = 도메인 모델·BC 경계는 측정 전 대화로 확정함(별도 보관). A단계는 그 위에서 시작.
- **첫 정직 런** = 빨라질 때까지 재시도한 값 아님. **막힌 구간·실패도 그대로 기록.**
- **측정 단위** = API 1개 (이 템플릿 G 자동화 단위 = 1 API = 1 워크트리 = 1 PR).

### 측정 대상 BC (락 확정)

| BC            | 상태         | 내용                                                   |
| ------------- | ------------ | ------------------------------------------------------ |
| `board`       | 🆕 신규      | Board + Post + Comment (레벨 판정이 board 안에서 끝남) |
| `user`        | 기존 확장    | Member.level 추가 — board가 LookupService로 읽음       |
| `admin`       | 재사용       | 최고관리자 인증/가드                                   |
| `file-upload` | 재사용(옵션) | 글 첨부 이미지 (OHS 소비)                              |

---

## 2. 단계별 기록 (프로세스 A~G)

> 벽시계 = 그 단계 시작~끝 실제 경과. 사람active = 사람이 실제로 손/머리 쓴 시간(에이전트 도는 동안 논 시간 제외).

| 단계                  | 주체            | 벽시계 | 사람active | 토큰/$ | 파일 | PR     | 통과 |
| --------------------- | --------------- | ------ | ---------- | ------ | ---- | ------ | ---- |
| A. SPEC 정립          | 사람(+에이전트) |        |            |        |      | —      | ✋   |
| B. DB 모델링          | 사람(+에이전트) |        |            |        |      | —      | ✋   |
| C·D·E (계약+Mock+E2E) | 에이전트        |        |            |        |      |        | ✋   |
| F. 도메인 + 인프라    | 에이전트        |        |            |        |      |        | ✋   |
| G. API×13 구현        | Opus + Sonnet   |        |            |        |      | \_\_개 |      |
| **합계**              | —               |        |            |        |      |        |      |

✋ = 사람 검토 게이트. 게이트 통과까지 걸린 시간도 해당 단계 벽시계에 포함.

---

## 3. G단계 API별 결과 (처리량 = 정직한 분모)

> 병렬 디폴트 4개. **실패는 숨기지 말고 그대로** — 4개 중 1개 실패도 데이터.

| API                              | 상태 | 벽시계 | PR  | 비고 (실패 시 5회 시도 후 남은 이유)    |
| -------------------------------- | ---- | ------ | --- | --------------------------------------- |
| POST /admin/boards               |      |        |     |                                         |
| PATCH /admin/boards/:id          |      |        |     |                                         |
| DELETE /admin/boards/:id         |      |        |     |                                         |
| GET /admin/boards                |      |        |     |                                         |
| PATCH /admin/users/:userId/level |      |        |     | **user 모듈 소유** (board 12 / user +1) |
| GET /boards                      |      |        |     | 회원용 게시판 목록                      |
| GET /boards/:id/posts            |      |        |     |                                         |
| GET /boards/:id/posts/:postId    |      |        |     |                                         |
| POST /boards/:id/posts           |      |        |     | 첨부 link (file-upload OHS 소비)        |
| PATCH /posts/:id                 |      |        |     |                                         |
| DELETE /posts/:id                |      |        |     |                                         |
| POST /posts/:id/comments         |      |        |     |                                         |
| DELETE /comments/:id             |      |        |     |                                         |

상태: `완료`(→PR) / `실패`(→이유) / `진행중`.

---

## 4. 결산 & 해석 (= 블로그 결론 / 영상 엔딩카드)

- **처리량**: ** API / **시간 = **시간당 \_\_ API**
- **비용효율**: $** / ** API = **API당 $\_\_**
- **시간 분해**: 사람 게이트 **% vs 에이전트 자동 **%
  - → _흥미로운 발견(채우기)_: 보통 "기계적 구현은 거의 공짜, 사람 판단(SPEC·스키마·검토)이 병목"
- **실패율**: ** / 13 (실패 사유 패턴: **)
- **재사용 효과**: 인증·업로드 재사용으로 절감한 추정 \_\_

### 영상 편집 매핑 (이 숫자를 자막/배속에)

- 에이전트 구간(G 병렬·빌드) → 16~20배속 + "🤖 자동" 자막
- 사람 게이트(A SPEC·B 스키마 판단) → 2~4배속 + "✋ 사람 판단" 자막
- 엔딩카드 = 위 TL;DR 표 그대로

---

## 측정 방법 메모 (재현용)

- **시간**: 화면 우상단 실시간 타이머 오버레이(원본 기준). 단계 전환마다 스플릿 기록.
- **토큰/비용**: 세션 종료 시 `/cost` 또는 사용량 대시보드에서 수집.
- **파일수**: `git diff --stat` 단계별 집계.
- **PR**: G 자동화가 생성한 PR 링크.
