<!--
  SPEC 작성 템플릿 — 새 기능을 시작할 때 이 폴더를 복사해 쓴다.
    cp -r docs/features/_TEMPLATE docs/features/<모듈명>

  SPEC은 SSOT(Single Source of Truth)다. 티켓·다른 문서와 충돌하면 무조건 SPEC을 따른다.
  (개발 프로세스 전체: docs/dev-process.md / A단계 산출물)

  작성 원칙:
  - 기능 요구사항은 "API 1개 = 작업 단위"로 쪼갠다 (G 단계가 API 단위로 병렬 구현).
  - 검증 규칙(길이/형식/상태 전이)·에러코드를 SPEC에 명시한다 → 도메인 VO·UseCase가 그대로 따른다.
    (추론 여지를 줄이는 게 핵심 — 모호하면 에이전트가 임의 추론한다)
  - <꺾쇠> 안은 채워넣을 자리. 가이드 주석(<!-- -->)은 작성 후 지운다.

-->

# <기능명> (<English Name>)

## 1. 배경 및 문제 정의

<!-- 지금 무엇이 불편하거나 없어서 이 기능이 필요한가. 현재 상태 → 문제 → 필요성 순으로. -->

<현재 상황과 해결하려는 문제를 2~3문단으로 서술>

---

## 2. 목표

<!-- 이 기능이 달성해야 하는 것. 측정/검증 가능한 문장으로. -->

- <목표 1>
- <목표 2>
- <목표 3>

---

## 3. 사용자 시나리오

<!-- 실제 사용 흐름. 역할(누가) + 단계(무엇을). 행복 경로 + 주요 예외 흐름. -->

### 시나리오 1: <역할이 무엇을 한다>

1. <단계>
2. <단계>
3. <결과 — 어떤 상태로 생성/전이되는지>

### 시나리오 2: <예외/분기 흐름>

1. <단계>
2. <단계>

---

## 4. API 개요 (계약 인덱스)

<!--
  엔드포인트 목록만 여기 둔다. 상세 계약(동작/검증/에러 체크리스트)은 같은 폴더의 api-spec.md.
  API 1개 = 한 행 = G 작업 단위. api-spec.md의 4.x 번호와 맞춘다.
  경로 prefix·역할·페이지네이션 방식 등은 고정하지 말고 기존 코드 조사로 맞춘다(rules/conventions §1).
-->

> 엔드포인트별 상세 계약(동작/검증/에러)은 **[`api-spec.md`](./api-spec.md)** 참조.

| #   | 동작        | 엔드포인트                                    | 권한   |
| --- | ----------- | --------------------------------------------- | ------ |
| 4.1 | <동작명>    | `POST /api/v1/<role>/<resources>`             | <역할> |
| 4.2 | <목록 조회> | `GET /api/v1/<role>/<resources>`              | <역할> |
| 4.3 | <상세 조회> | `GET /api/v1/<role>/<resources>/:id`          | <역할> |
| 4.4 | <상태 변경> | `PATCH /api/v1/<role>/<resources>/:id/status` | <역할> |

---

## 5. 범위

### 포함

- <이번에 만드는 것 나열>

### 미포함

<!-- 일부러 안 만드는 것. "나중에 할 수도 있는 것"을 명시해 스코프 크리프 방지. -->

- <제외 항목>

---

## 6. 전제 조건 및 제약사항

<!-- 구현이 의존하는 가정·규칙. 모호하면 여기서 명확히 정해 추론을 없앤다. -->

- <예: user/와 admin/ 엔드포인트는 동일 UseCase 공유, 컨트롤러만 분리>
- <예: enum 고정값 — STATE_A / STATE_B / ...>
- <예: 파일 첨부는 기존 업로드 플로우 활용, 최대 N개>
- <예: 소유권 — 본인 리소스만 접근>

---

## 7. 수락 기준

<!-- "이게 다 되면 완료"의 체크리스트. E2E TC와 1:1로 대응되게. -->

- [ ] <검증 가능한 완료 조건 1>
- [ ] <검증 가능한 완료 조건 2>
- [ ] <에러 케이스가 의도대로 동작>

---

## 8. 미결 사항

<!-- 아직 결정 못 한 것. 비워둬도 됨. -->

| 항목   | 상태 | 비고   |
| ------ | ---- | ------ |
| <항목> | 미정 | <설명> |
