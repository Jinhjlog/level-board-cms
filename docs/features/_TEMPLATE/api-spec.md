<!--
  API Spec 템플릿 — SPEC.md의 계약 산출물. SPEC.md(배경·목표·범위)와 짝으로 둔다.
    docs/features/<모듈>/SPEC.md       ← 배경·목표·시나리오·범위·제약 (왜)
    docs/features/<모듈>/api-spec.md   ← 엔드포인트별 계약 (무엇을·어떻게) ← 이 파일

  작성 원칙:
  - API 1개 = 한 섹션 = G 작업 단위. SPEC.md 4장(인덱스)과 번호를 맞춘다.
  - 동작/검증/에러를 체크리스트(- [ ])로 빠짐없이. 추론 여지를 줄이는 게 핵심.
  - **완전한 Request/Response DTO 스키마까지는 적지 않는다** — 필드·제약·에러코드 수준의 가벼운 계약.
    (정확한 스키마는 D단계 Mock·구현에서 확정)
  - 경로 prefix·역할·ID 타입·페이지네이션 방식 등은 고정하지 말고 기존 코드 조사로 맞춘다(rules/conventions §1).
  - <꺾쇠> 안은 채워넣을 자리. 가이드 주석(<!-- -->)은 작성 후 지운다.

-->

# <기능명> — API Spec (<English Name>)

> SPEC.md(배경·목표·범위)의 계약 산출물. API 1개 = 한 섹션 = G 작업 단위.

## 4.1 <동작명> (`POST /api/v1/<role>/<resources>`)

- [ ] <인증/권한 요구사항>
- [ ] <입력 필드>를 입력받는다 (<제약: 길이/형식/필수 여부>)
- [ ] <비즈니스 규칙 / 검증>
- [ ] <성공 응답: 상태코드 + 반환 내용>
- [ ] <실패 케이스: 상태코드 + `ERROR_CODE`>

## 4.2 <목록 조회> (`GET /api/v1/<role>/<resources>`)

- [ ] <권한>
- [ ] <조회 범위: 본인 것만 / 전체 등>
- [ ] 정렬: <기준>
- [ ] 페이지네이션: <커서 / 오프셋> (기본 N건, 최대 M건) <!-- 방식은 기존 코드 조사 -->
- [ ] 필터: <상태/카테고리 등>
- [ ] 각 항목 포함 필드: <나열>

## 4.3 <상세 조회> (`GET /api/v1/<role>/<resources>/:id`)

- [ ] <권한 / 소유권>
- [ ] 포함 필드: <나열>
- [ ] 없거나 권한 없으면 404 + `<RESOURCE>_NOT_FOUND`

## 4.4 <상태 변경> (`PATCH /api/v1/<role>/<resources>/:id/status`)

<!-- 상태가 있는 기능이면 전이 규칙을 표로 명시 (도메인이 그대로 따른다) -->

**상태 전이 규칙:**

| 현재 상태 | 변경 가능 상태 |
| --------- | :------------: |
| <STATE_A> |   <STATE_B>    |
| <STATE_B> |   <STATE_C>    |
| <STATE_C> |   변경 불가    |

- [ ] 허용되지 않은 전이 시도 시 400 + `INVALID_STATUS_TRANSITION`
